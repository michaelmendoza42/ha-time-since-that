# Agent instructions

This repo is public/shareable. Do not commit secrets or machine-specific HAOS access details.

For Michael's local Home Assistant deployment, read the private local notes in
`LOCAL.md` if present.

If `LOCAL.md` references `macos-management`, use that repo's approved `ssh-audit`
flow and never run ad hoc SSH.
