mark_done:
  name: Mark item done
  description: Record a completion event for an item and update its freshness stats.
  target:
    entity:
      integration: time_since_that
  fields:
    chore_id:
      name: Item ID
      description: Stable item ID from Time Since That configuration. Use this if not targeting an entity.
      required: false
      selector:
        text:
